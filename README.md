# outside-disconnect
Outside Disconnect: Minneapolis emergency-disconnect checklist. Ask if the shutoff is at the meter or a separate outdoor box, and whether a panel swap triggers it. Customer never pays the directory.
